#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(size_t i = 0; i < (n); ++i)
using ll = long long;

int main() {
    int n;
    cin >> n;
    vector<int> distance(n);
    rep(i, n) cin >> distance[i];
    
    int size = 0;
    rep(i, )
}