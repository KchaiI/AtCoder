#include <bits/stdc++.h>
using namespace std;

int main() {
    int n = 0, count = 0;
    cin >> n;

    //数値入力
    for(int i = 0; i < n; i++) {
        cin >> buttun[n];
    }

    //判定
    cout << buttun[1];
}